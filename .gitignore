# 빌드 산출물
firmware/build/
firmware/v0.0.1/build/
*.o
*.d
*.lst
*.map
*.elf

# 벤더 트리 (ST / FreeRTOS 배포본을 그대로 복사해 넣는 자리)
# 받는 방법은 FLASH_INSTRUCTIONS.md 를 보세요.
firmware/Drivers/STM32F1xx_HAL_Driver/
firmware/Drivers/CMSIS/
firmware/Middlewares/Third_Party/FreeRTOS/

# 에이전트 · 에디터 로컬 설정
.claude/

# Windows 예약 이름(널 디바이스)으로 잘못 생성된 파일.
# 읽을 수 없어 git 이 "short read while indexing nul" 을 낸다.
nul
